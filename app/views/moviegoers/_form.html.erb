<%= form_for(@moviegoer) do |f| %>
  <% if @moviegoer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moviegoer.errors.count, "error") %> prohibited this moviegoer from being saved:</h2>

      <ul>
      <% @moviegoer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :uid %><br>
    <%= f.text_field :uid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
